apiVersion: apps/v1
kind: Deployment
metadata:
  name: fastapi-app2
  labels:
    app: fastapi-app2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fastapi-app2
  template:
    metadata:
      labels:
        app: fastapi-app2
    spec:
      containers:
        - name: fastapi-app2
          image: fastapi-app2:latest
          imagePullPolicy: Never # Use local image
          ports:
            - containerPort: 8000
              protocol: TCP
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
